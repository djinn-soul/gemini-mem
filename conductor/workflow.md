# Workflow

1. Develop and test locally via extension linking.
2. Verify hook contracts (stdin JSON, stderr logs, stdout JSON only).
3. Validate storage/retrieval behavior with command-level diagnostics.
4. Release via git ref channels (`main`, `dev`, tags).
